# infra-drift-detection-demo
Drift detection system using Terraform and GitHub Actions where plans are executed periodically. If any drift is detected, the system triggers alerts via Slack and logs the change.
